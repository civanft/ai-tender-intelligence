# This application uses the public TED API and needs NO API key.
# Do not invent or add an OpenAI/GCP credential for the current application.
# Future local credentials belong in .env (git-ignored, chmod 600).
# The application does not automatically load .env.
# Deployment uses GitHub OIDC, not a service-account JSON key.
